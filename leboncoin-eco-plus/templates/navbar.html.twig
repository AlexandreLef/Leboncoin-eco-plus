{% block stylesheet %}
    <link rel="stylesheet" href="/assets/css/navbar.css">
{% endblock %}



<nav class="custom-nav shadow-sm">
    <div class="nav-autoresize">
        <div class="nav-left">
            <a href="{{ path('home') }}"><img alt="Logo leboncoin" class="nav-logo" src="/assets/img/logo.svg"/></a>
            <a href="{{ path('product_add') }}" class="btn btn-primary semi-bold" type="submit"><i class="bi bi-plus-square"></i> Déposer une annonce</a>
            <a class="nav-button search-button semi-bold" href="{{ path('product_list') }}">
                <i class="bi bi-search"></i>
                <p>&nbsp;Rechercher</p>
            </a>
        </div>
        <div class="nav-buttons">
            <a class="nav-button" href="{{ path('favorite') }}">
                <i class="bi bi-heart"></i>
                <p>Favoris</p>
            </a>
            <a class="nav-button" href="{{ path('message') }}">
                <i class="bi bi-chat-right-text"></i>
                <p>Messages</p>
            </a>
            {% if is_granted('ROLE_USER') %}
                <a class="nav-button" href="{{ path('account') }}">
                    <i class="bi bi-person"></i>
                    <p>{{ app.user.firstname }}</p>
                </a>
            {% else %}
                <a class="nav-button" href="{{ path('account_login') }}">
                    <i class="bi bi-person"></i>
                    <p>Se connecter</p>
                </a>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
                <a class="nav-button" href="{{ path('admin') }}">
                    <i class="bi bi-gear"></i>
                    <p>Options admin</p>
                </a>
            {% endif %}
        </div>
    </div>
</nav>